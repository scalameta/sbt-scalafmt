updates.pin = [
  { groupId = "org.scala-lang", artifactId = "scala-library", version = "2.12." }
  // meta-build must stay on sbt 1.x: sbt 2.x can't cross-publish the sbt 1.x plugin
  { groupId = "org.scala-sbt", artifactId = "sbt", version = "1." }
]
